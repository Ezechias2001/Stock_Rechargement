<form method="post" action="{{ path('app_recharge_delete', {'id': recharge.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recharge.id) }}">
    <button class="btn btn-danger mt-2"> <i class="bi bi-trash mx-2"></i>Supprimer</button>
</form>
